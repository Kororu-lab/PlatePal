import SwiftUI

// Module definition to ensure files can see each other
public enum PlatePal {
    // Empty - just used as a namespace
}

// Module-level access for main app
public func startApp() -> some View {
    ContentView()
} 